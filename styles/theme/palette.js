import { Colors } from "../../constants/Colors";

export const Palette = {
    primary: {
        main: Colors.primary,
    },
    secondary: {
        main: Colors.secondary,
    },
    info: {
        main: Colors.grey,
    },
    background: {
        default: Colors.light,
        paper: Colors.light
    }
}